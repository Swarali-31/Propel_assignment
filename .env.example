# Optional. Leave OPENAI_API_KEY empty to use deterministic template briefings.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Backend (usually set by docker-compose)
# DATA_DIR=/data
# DATABASE_URL=sqlite:////data/kspdb.db
# SEED_ON_STARTUP=true
# CORS_ORIGINS=*
